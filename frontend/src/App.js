import React from 'react';

// Estilos global ou reset própio
import './global.css';

// importamos as rotas de routes.js
import Routes from './routes';
function App() {
  return (

    // Aqui chamamos o componente criado
    // Com o title = Chamamos a propidedades
    <Routes />
  );
}

export default App;
